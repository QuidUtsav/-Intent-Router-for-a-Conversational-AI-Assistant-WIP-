# -Intent-Router-for-a-Conversational-AI-Assistant-WIP-
This repository contains the early brain of a Jarvis-style personal assistant — an intent router that decides how the assistant should respond to user input.  Instead of blindly replying to everything like a chatbot, this system classifies user intent and routes queries to the appropriate capability (conversation vs information lookup).
